import React from 'react';

export const Input = () => {
    return (
            <React.Fragment>
                   <input placeholder={''} type={''} />
            </React.Fragment>
    )
}